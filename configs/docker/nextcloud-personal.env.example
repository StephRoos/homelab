# ============================================================================
# Nextcloud Personal — secrets template
# ============================================================================
# Copy to `nextcloud-personal.env` (gitignored) and fill with real values.
# Generate strong secrets with:  openssl rand -base64 32
#
#   cp nextcloud-personal.env.example nextcloud-personal.env
#   # then edit nextcloud-personal.env
# ============================================================================

NEXTCLOUD_DB_PASSWORD=
NEXTCLOUD_REDIS_PASSWORD=
NEXTCLOUD_ADMIN_USER=steph
NEXTCLOUD_ADMIN_PASSWORD=
